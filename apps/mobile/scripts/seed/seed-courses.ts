/**
 * Course seed script — future seed entry point for Manila Golf and Country Club and later courses.
 *
 * Expected inputs/outputs: optional seed options in, inserted course summary out.
 * Supabase target (future): courses table.
 * Status: PLACEHOLDER — not wired to Supabase yet.
 */
/** Seed courses. Will shell out to the Supabase CLI or use MCP after the course schema exists. */
export async function seedCourses(): Promise<void> {
  // TODO(supabase): insert MGCC and future course seed data only after migrations exist.
  throw new Error('Not implemented');
}

